{
  "name": "simple-calendar-app",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .js"
  },
  "dependencies": {
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.0.0",
    "eslint": "^8.0.0",
    "jsdom": "^27.4.0",
    "vite": "^4.0.0",
    "vitest": "^1.0.0"
  }
}
